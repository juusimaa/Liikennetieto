<UserControl x:Class="Liikennetieto.ParkingStationViews.ParkingStationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Liikennetieto.ParkingStationViews"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance {x:Type local:ParkingStationViewModel}}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Parking station:" FontSize="14" Margin="5"/>

        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Stations}" DisplayMemberPath="Name" 
                  SelectedItem="{Binding SelectedStation}" Margin="5" FontSize="14"/>

        <local:MapView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" DataContext="{Binding MapViewModel}"/>

    </Grid>
</UserControl>
